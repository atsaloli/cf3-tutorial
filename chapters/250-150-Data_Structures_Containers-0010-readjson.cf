bundle agent main {

  vars:
      "mydata"
        data => parsejson('{"Pizza" : "Pepperoni"} ');

      "keys"
        slist => getindices("mydata");

  reports:

      "$(keys)  $(mydata[$(keys)])";
}
