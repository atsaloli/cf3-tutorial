# run this in verbose mode and notice the comment

bundle agent main {

  files:

      "/tmp/testfile"


        handle => "mk_file",
        comment => "Create a vital file, needed for XYZ.",
        create  => "true";
}
