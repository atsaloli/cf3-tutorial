# Example of a list value in CFEngine policy

bundle agent main {

  processes:

      "eggdrop"  # IRC bot, see https://www.eggheads.org/

        comment => "We don't want IRC bots on our Web servers",
        signals => { "term", "kill" };

}

