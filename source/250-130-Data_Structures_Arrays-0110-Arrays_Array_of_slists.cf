# Referring to an slist in scalar context implies looping.
#
# In this example, we set up a nested implicit loop by referring to TWO
# slists in scalar context

bundle agent main
{
  vars:
      "config[users]"
        slist => { "jim", "jane", "george" };

      "config[packages]"
        slist => { "httpd", "php" };

      "keys"
        slist => getindices("config");

  reports:
      "The value for key $(keys) is: $(config[$(keys)])";
}
