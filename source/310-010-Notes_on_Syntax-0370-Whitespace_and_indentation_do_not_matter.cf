# Whitespace/indentation does not matter:
# both of below work just fine.
#
# Whitespace is for readability.

bundle agent main
{
  methods:
      "no_whitespace";

  reports:
      "With whitespace";
}

bundle agent no_whitespace {reports:"Without whitespace";}
