# demonstrate handle conflict (try to re-use a promise handle)

bundle agent main
{
  files:
      "/tmp/testfile"
        handle => "create_testfile",
        create  => "true";

  reports:
     "hello world"
       handle => "create_testfile";
}
