# soft classes set in agent bundle have bundle scope by default

bundle agent main
{
  classes:
    any::
      "myclass"
        scope => "namespace";

  methods:
    "bundle_2";
}

bundle agent bundle_2
{
  reports:
    myclass::
      "Yay!  myclass is set";
}

