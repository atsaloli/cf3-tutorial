# Classes set in "common" bundles have global scope by default

bundle common global_definitions
{
  classes:
    any::
      "myclass";
}

bundle agent main
{
  reports:
    myclass::
      "Yay!  myclass is set";
}

