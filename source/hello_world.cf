body common control {
  bundlesequence => { "hello_world" };
}

bundle agent hello_world
{

reports:

    "$(sys.date)";

#  vars:
#    "myclasses"
#      slist => classesmatching(".*day");
#
#  reports:
#    
#      "$(myclasses)";

  methods:
    "whatever"
      usebundle => second;

  methods:
    "whatever"
      usebundle => second;

} 

bundle agent second {

  reports:

    "This is a reports promise";

}
